.panel.panel-primary.junior-events
  .panel-heading
    .text-center= t('woman.international_events')
  - @international_events.each do |event|
    .panel-body
      = link_to event.name, event
      %br
      = event.dates
      %br
      = ((event.note || '').strip.split("\n").first || '').strip
  .panel-footer.text-center
    = link_to "More international events", events_path(category: "wint")
  