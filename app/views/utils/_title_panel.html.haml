- defined?(header_text) || header_text = "undefined"

%section{class: "full-width title-only"}
  .container
    .row
      .col-md-12.flex-row
        .form-panel
          .inline-between
            %h2= "#{header_text}"
            = yield
