- radio_options = (include_all ? [["all", t("all")]] : []) + options
- checked_value = checked.presence || radio_options.first&.first

.inline-radio
  - radio_options.each do |value, label|
    - input_id = "#{name}_#{value}"
    - is_checked = value.to_s == checked_value.to_s
    %input{type: "radio", name: name, id: input_id, value: value, checked: (is_checked ? "checked" : nil)}
    %label{for: input_id}= label
