- if sam = Image.where(id: 1182).first
  .panel.panel-primary
    .panel-heading
      .text-center.small Stephen Brady, Phillip Short
    .panel-body= sam.expand(type: "IMG", align: "center", responsive: "true", alt: "Stephen Brady, Phillip Short").html_safe
    .panel-footer
      .text-center.small 2015 Champions

- if mg = Image.where(id: 1239).first
  .panel.panel-primary
    .panel-heading
      .text-center.small M. Gedvilaite
    .panel-body= mg.expand(type: "IMG", align: "center", responsive: "true", alt: "Monika Gedvilaite").html_safe
    .panel-footer
      .text-center.small 2015 Women's Champion

.panel.panel-primary
  .panel-body
    .text-center.small
      Apart from its two websites,
      %strong www.icu.ie
      ,
      = succeed(",") do
        %strong ratings.icu.ie
      and a <a href="https://www.facebook.com/IrishChess">Facebook page</a>
      the ICU is not responsible for any other internet media relating to Irish chess.

